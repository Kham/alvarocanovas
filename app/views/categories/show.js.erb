$("#content")
  .fadeOut('slow',
    function(){
      $(this)
        .html('<%=  render_j :partial => 'show' %>')
        .fadeIn()
        .delay(10000);
        $.get('<%= category_pictures_url(@category)%>');
    }
  );



